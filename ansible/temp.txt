    #- name: Check if c path exists
    #  stat: path=/c
    #  register: check_c_path

    #- name: Move apr to httpd/srclib
    #  command: mkdir 
    #  when: not check_c_path.stat.exists

    #creates could be useful:
    #command: mv /var/cache/apt/archives/*.deb {{ archive_path }}/updates/ creates={{ archive_path }}/foo.txt

